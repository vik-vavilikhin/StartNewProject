import * as app from './modules/app';

document.addEventListener('DOMContentLoaded', () => {
  'use strict';

  app.inspectUserAgent();
  app.testWebP();
  app.backgroundImage();
  app.burgerActive();
  app.elemReplace();
});